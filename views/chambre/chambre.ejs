<%- include('../common/header.ejs') %>
<main>
    <h1>Vous êtes dans la chambre n° <%= chambre %></h1>
    <p>Vous êtes à l'étage n°<%= etage %></p>

    <div>
        <p>Voici les locataires de la chambre :</p>
        <ul>
            <% for(let el of tableau) { %>
                <li>
                    <%= el %>
                </li>
            <% } %>
        </ul>
    </div>
</main>
<%- include('../common/footer.ejs') %>

